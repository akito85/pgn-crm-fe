// src/components/Nx/NxTable/constants.js

// ── Visual tokens ────────────────────────────────────────────────────────────
export const HEADER_BG   = '#2C6FAD';
export const BORDER_COL  = '#C8CDD4';
export const ROW_WHITE   = '#FFFFFF';
export const ROW_HOVER   = '#EBF2FA';
export const FONT_FAMILY = "'PlusJakartaSans', 'PublicSans', sans-serif";

// ── Timing ───────────────────────────────────────────────────────────────────
export const RESIZE_RESET_MS   = 100;
export const SEARCH_DEBOUNCE_MS = 220;
export const PREFS_DEBOUNCE_MS  = 400;

// ── Dimensions ───────────────────────────────────────────────────────────────
export const MIN_COL_WIDTH_PX   = 50;
export const DEFAULT_COL_WIDTH  = 150;
export const FOOTER_HEIGHT_PX   = 33;
export const TOOLBAR_HEIGHT_PX  = 48;

// ── Scroll ────────────────────────────────────────────────────────────────────
export const SCROLL_THRESHOLD_PX = 80;   // px from bottom before Phase B fires
export const SCROLL_MIN_PX       = 80;   // minimum usable scroll area for fixed-col warning

// ── Prefs persistence ────────────────────────────────────────────────────────
export const PREFS_VERSION = 1;

// ── Pinned columns ────────────────────────────────────────────────────────────
export const NO_COL_KEY           = 'no';
export const NO_COL_WIDTH         = 90;
export const ACTION_COL_KEY       = 'action';
export const ACTION_COL_MIN_WIDTH = 111;
