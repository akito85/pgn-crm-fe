import ScheduledJobsPanel from "./ScheduledJobsPanel";

// JobSchedulePage now lives inside the tabbed Job Monitoring page as a panel.
// This thin re-export keeps any direct route/import referencing JobSchedulePage working.
export default ScheduledJobsPanel;
