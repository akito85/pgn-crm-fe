import ExecutionsPanel from "./ExecutionsPanel";

// JobExecutionPage now lives inside the tabbed Job Monitoring page as a panel.
// This thin re-export keeps any direct route/import referencing JobExecutionPage working.
export default ExecutionsPanel;
